### KUBELET, CONTAINERD, ECTD - are installed as Systemd Services

Once **Kubelet** is installed - it will take care of the **installation of the other Kubernetes components as Pods in Kube-System Namespace** 
